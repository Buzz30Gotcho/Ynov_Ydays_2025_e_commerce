{
  "name": "ydays-backend",
  "version": "0.1.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "fuser -k 4000/tcp 2>/dev/null || true; nodemon server.js",
    "dev": "nodemon server.js",
    "start:clean": "fuser -k 4000/tcp 2>/dev/null || true; nodemon server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}